# 全局规则

- 所有对话和输出必须使用中文
- 创建自定义清单弹窗中的检索条件必须与主数据清单（DataListTab.vue）上方的检索条件保持同步，包括条件字段、选项列表、交互方式等
- 任何涉及删除数据库数据的操作前，必须向用户明确确认，列明即将删除的具体数据（包括但不限于：表名、记录ID、关键字段值），未经用户明确确认不得执行删除操作
- 任何涉及修改数据库数据的操作前（包括但不限于增删改），必须向用户明确确认，列明即将执行的具体操作、涉及的表名、记录ID、关键字段值，除非用户明确指令可以直接操作数据库，否则未经用户确认不得执行
- 每次完成代码修改后，必须进行严格验证（前端构建、后端编译、功能逻辑检查），确认功能真正实现后再结束工作
- 每次重启前后端服务后，必须验证两个服务均正常响应（curl 检查 8080 和 5173 端口返回 200），如失败则重新启动直至成功
- 每次代码修改完成后，必须检查前后端是否能正常运行：前端执行 npm run build 确认无构建错误，后端执行 mvn compile 确认无编译错误，然后重启服务并用 curl 验证端口响应正常，确认页面无 500/400 等接口报错后才可结束
- 每次重启前后端服务后，除了 curl 验证端口可达外，还必须用 curl 调用关键业务接口（如版本列表 /api/versions、登录接口等）检查是否返回 500 错误。若出现 500 错误，必须排查后端日志定位原因并修复后重新验证，不允许带着 500 错误结束工作
- 仅涉及数据表结构变更的操作，才需要在工程目录下 db_changes/ 目录存储对应的变更脚本文件，脚本文件名应包含时间戳和简要描述
- 当用户明确当前研发版本号时，必须在 VERSION.md 最上方新增版本号块（格式：`# 当前研发版本: Vx.x.x`），旧版本内容保留在下方并用分隔线 `---` 隔开
- 每次完成功能变更后，必须在当前版本号下方的变更说明列表中追加本次变更的简要说明
- 注意区分"研发版本号"（如 V1.0.2，指平台软件版本，存在 VERSION.md 中）和"清单版本号"（如 1.0、1.1.2，指产品数据版本，存在 data_version 表中），两者完全独立
- VERSION.md 的变更说明必须按系统菜单的目录层级组织，一级分类为菜单项（产品清单、需求管理、系统管理），二级为子菜单（需求管理下分需求清单/需求图片，系统管理下分用户管理/权限套餐管理/基础数据维护），基础数据维护下再分业务分类维护/解决方案维护/应用角色维护/功能状态维护；每条变更说明归入对应的菜单层级下
