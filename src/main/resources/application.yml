server:
  port: 8080
  tomcat:
    connection-timeout: 600000

logging:
  level:
    com.superpower: DEBUG
    org.springframework.security: DEBUG

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  jackson:
    time-zone: Asia/Shanghai
  datasource:
    url: jdbc:sqlite:superpower.db?busy_timeout=30000
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: com.superpower.config.SqliteDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: superpower-test-secret-key-for-jwt-token-generation-2026
    expiration-ms: 86400000
  doc-storage-path: ./generated-docs
