# 当前研发版本: V1.0.5 beta

## 功能变更说明

### 产品清单
- 批量修改版本划分：批量操作菜单新增"版本划分"选项，支持批量设置曜/远/驰系列及对应最小集属性
- 图片重命名同步物理文件：重命名图片时同步修改磁盘文件名，确保物理文件名与显示名一致
- 非常规操作新增"同步图片文件名"：一次性将现有图片物理文件名同步为显示名

#### 图床管理
- 图片排序优化：图床和编辑器图片选择器按上传时间倒序排列，最新图片显示在最前面
- Word生成修复：修复本地图片（local:前缀）未正确嵌入Word文档的问题
- Word标题修复：修复图片重命名后Word中图片标题仍显示旧物理文件名的问题（改为使用显示名）
- 图片重命名引用同步修复：修复图床改名后自定义编辑器中图片丢失的问题，改用 data-id 精确定位 image-card 块进行引用同步，彻底解决历史数据 URL 格式不一致导致替换失败的问题

### 系统管理
#### 图床管理
- 图片加载遮罩：图床切换目录时增加加载等待遮罩效果，分批渲染避免动画卡顿
- 图片版本隔离架构改造：图片物理文件按版本ID独立存储，彻底解耦版本间文件依赖
  - 新增"非常规操作"菜单：提供图片版本隔离迁移功能（一键迁移/分步操作），支持迁移进度实时查看
  - 上传图片增加同版本同目录同名文件的唯一性检查
  - 创建新版本时物理文件独立复制，不再多版本共享同一文件
  - 删除图片时直接删除物理文件，不再需要跨版本引用检查

---

# V1.0.4 beta

## 功能变更说明

### 产品清单
- 预览性能优化：图片宽高信息存储到数据库，预览时直接从数据库读取，不再逐张HTTP下载图片获取尺寸，大幅提升加载速度
- 预览图片布局修复：修复连续竖版图片未并排排列的问题（尺寸判断从HTTP下载改为数据库读取，启动时自动回填已有图片宽高）
- 数据加载优化：主清单和自定义清单加载时增加"数据加载中"提示
- Word生成修复：本地图片改为从文件系统直接读取，修复部署到内网后Word中图片丢失的问题
- 图床计数修复：修复图床目录树产品节点图片计数不准确的问题（与右侧实际显示数量保持一致）
- 文档生成优化：功能说明文档生成增加"包含图片"复选框，取消勾选后生成纯文字Word文档
- 批量操作优化：点击确认后立即关闭操作弹窗，避免批量处理期间用户二次操作

---

# V1.0.3 beta

## 功能变更说明

### 产品清单
- 批量修改解决方案弹窗：增加替换/追加模式切换，替换模式覆盖原有值，追加模式将新值添加到原有解决方案中
- 清单编辑表单：业务分类、业务域下拉选择器改为所有级别可编辑
- 清单编辑表单：新增系统类型下拉选择器（数据来源于系统类型维护）
- 图片名称同步修复：图床重命名图片后，编辑器 image-card 中的名称自动同步更新
- 编辑器打开/保存时图片名称校准：自动将 image-card 的 filename 与 image_resource 表最新记录对齐，防止旧名称覆盖
- 预览图片修复：修复 image-card 格式 URL 含中文空格时预览不显示的问题（URL 空格编码为 %20）
- 预览 caption 修复：修复 inline URL caption 含空格时被截断的问题
- 版本切换弹窗修复：修复点击当前版本仍弹出确认框的问题
- 引用展示增强：图片引用列表新增版本号列，区分不同版本的引用
- 拖拽交互重构：拖拽层级变更改为鼠标左右移动控制——向左升级（绿色）、向右降级（橙色），指示线统一显示在行底部
- 拖拽升级修复：修复 L4→L3 升级操作不生效的问题（L3 节点 parent_id 为空时的 fallback 处理）
- 拖拽域校验修复：修复 findL2Ancestor 对 L3 节点返回 null 导致同域拖拽误报"只能在同一业务域内移动"的问题
- 拖拽层级变更后同步更新 image_resource 的 category/domain/product，修复图床计数为 0
- 预览图片URL修复：去掉硬编码 localhost:8080，改用相对路径，支持部署到任意服务器
- 预览图片说明文字：自动去除文件扩展名，只显示文件名主体
- 上传图片自动补扩展名：用户输入的显示名无扩展名时自动追加
- 插入图片后滚动定位：插入/替换图片后编辑器自动滚动到图片位置
- 拖拽升级修复：修复 L4→L3 升级操作不生效的问题（L3 节点 parent_id 为空时的 fallback 处理）
- 拖拽域校验修复：修复 findL2Ancestor 对 L3 节点返回 null 导致同域拖拽误报"只能在同一业务域内移动"的问题

### 需求管理
- 图片重命名时同步更新需求条目（ReqItem）中的引用名称

### 系统管理
#### 基础数据维护
##### 产品分类维护
- 产品分类改为两级结构：统计分类（L1）→ 核心业务产品（L2）
- 修正Excel导入逻辑：L2映射到第3列（核心业务产品），跳过第2列
- Excel导入同时导入系统归类到sys_option表（type=systemType）
- 支持拖拽改变显示顺序

##### 系统类型维护
- 新增系统类型维护菜单，维护系统归类选项（如：科研、数据管理、AI、集成等）
- 复用选项维护组件（OptionManage.vue），type=systemType
- Excel导入产品分类时同步导入系统类型数据

#### 图床管理
- 引用查询性能优化：图片引用查询从 findAll 全量加载改为 SQL LIKE 查询，大幅提升响应速度
- 引用按钮增加 loading 状态，防止用户误以为无响应
- 图床目录树修复：修复部分 L3 产品节点未显示在树中的问题（L3 节点 domain_id 为空时通过 colBizDomain 文本匹配 fallback）
- ImagePicker 点击左侧树节点时自动清空搜索框

---

# V1.0.2

## 功能变更说明

### 产品清单
- 数据层级修复：根据编号前缀段数自动修正 level、parent_id、is_leaf、domain_id、category_id
- 批量修改业务域弹窗：增加 L2 下子树选择功能，支持将选中条目移动到指定树节点下
- 批量修改业务域弹窗：修复子树不显示问题（后端改为递归加载完整树结构，前端去掉lazy模式改为一次性渲染）
- 批量修改业务域修复：不选树节点时只修改分类和域信息，不再错误地改变条目层级
- 批量修改业务域简化：移除弹窗中的L3级树列表，只保留业务分类和业务域选择
- 拖拽改变层级：在同一业务域下支持拖拽改变树层级
- 拖拽层级上移：支持将L4+节点拖回L3同级（sibling同级/nest移入子节点）
- 拖拽目标检测重构：改用滚动位置+行高纯计算确定目标行，修复虚拟滚动下拖拽只有第一行生效的问题
- 拖拽示意线：飞书风格动态缩进（排序=蓝色当前行缩进，下移层级=橙色多缩进一级，上移层级=绿色少缩进一级）
- 清单构建排序：后端查询改为按基础数据中分类/域的sort_order排序，而非字符串字典序
- 清单排序修复：前端buildTree不再对roots全局排序（sortOrder仅在同一L2内有效），分隔行顺序严格按基础数据的分类/域排序
- 自定义Tab排序修复：customTabEntry.sortOrder相同时按分类/域sort_order二级排序
- 左侧导航树排序修复：L1/L2分别按base_category/base_domain的sort_order排序
- 批量修改业务域后排序：被移动条目自动排到目标L2域末尾（sortOrder = 目标域L3最大值+1）
- 拖拽到域末尾修复：拖拽到同域最后一行下方时正确显示蓝色示意线并插入末尾
- 自定义Tab排序修复：hasNullSort分支初始排序改为按分类/域sortOrder，最终排序改为先按分类/域分组再按customTabEntry.sortOrder排序

### 系统管理
#### 用户管理
- 403响应处理：token过期时自动清除并跳转登录页，避免请求静默失败

#### 基础数据维护
##### 业务分类维护
- 登录页面版本号：从后端动态读取 VERSION.md，不再硬编码
