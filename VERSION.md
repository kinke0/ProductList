# 当前研发版本: V1.0.9 beta

## 功能变更说明

### 产品清单
- 自定义清单粘贴修复：前端 copyEntries/moveEntries API 增加 customTabId 参数；后端 copyEntriesToTarget/moveEntriesToTarget 增加 customTabId 参数，完成后将新节点插入 custom_tab_entry
- 复制粘贴 bug 修复：修复多选复制时子节点重复克隆的问题（过滤嵌套的 sourceIds）；修复复制到自身下级导致无限递归（增加自引用检查和递归深度保护 MAX_CLONE_DEPTH=20）；修复 @Transactional 位置错误导致异常时部分数据不回滚
- 粘贴操作增加全屏遮罩提示，防止用户重复操作
- 跨版本复制：支持从已发布版本复制数据到编辑中版本，复制时自动克隆图片文件和 image_resource 记录，更新描述中的 data-url 和 data-id
- 复制/剪切图片克隆逻辑优化：同 L3 产品内复制不克隆图片（避免重名冲突）；不同 L3 产品间复制克隆图片确保各自独立编辑；剪切移动时不同 L3 产品间自动移动图片文件
- 已发布版本右键菜单：只显示"复制"按钮，隐藏剪切/粘贴/升级/降级/上移/下移
- 已发布版本工具栏：隐藏所有修改操作按钮，只保留文档生成、查询、重置、展开/折叠

### 需求管理
（无变更）

### 系统管理
- 非常规操作新增"版本操作"页签：将导入本地Excel、迁移图片、修复层级三个操作从产品清单工具栏迁移到非常规操作页面的独立页签中，增加版本选择器，操作界面更清晰
- 产品清单工具栏精简：移除导入本地Excel、迁移图片、修复层级三个按钮，这些操作统一在非常规操作页面执行
- 备份数据库修复：改用 SQLite backup to 命令替代 Files.copy，解决服务器上数据库被活跃连接占用时备份失败的问题；备份后自动 VACUUM 压缩主库，防止数据库文件体积膨胀

### 文档生成
- 修复服务器上文档生成（Excel + Word）永远卡在 generating 状态的问题：文档生成线程池从单线程改为3线程，避免任务排队阻塞；HikariCP 增加 connection-timeout 和 leak-detection 配置；生成流程各环节添加关键日志；Excel 进度100%移到序列化完成之后；updateGenRecordSuccess 重试增强（3次→10次，间隔200ms→1s）
- Excel生成格式调整：原来"曜系列最小集/驰系列最小集/远系列最小集"三列改为"曜/曜最小集/驰/驰最小集/远/远最小集"六列，新增的曜/驰/远列从版本划分字段判断系列归属显示√，最小集列显示是/否
- Excel生成排序修复：自定义清单和版本全量生成改用已排序的查询方法（findEntriesByTab/findAllEntries），排序逻辑与前端一致（base_category.sortOrder → base_domain.sortOrder → level → parentId → sortOrder），不再使用编号前缀排序
- Excel生成内容不全修复：分组时递归向上查找父节点的分类/域信息，避免条目自身分类域为空时丢失
- Excel最小集列显示优化：系列被选中时最小集列显示"是"或"否"，系列未被选中时最小集列留空
- Excel生成状态修复：修复删除旧生成记录后再生成时任务被误判为已取消导致卡在"生成中"的问题（cancelledRecords标记残留）；修复result为null时状态永远停留在generating的问题

#### 图床管理
- 图片目录超L3层级数据修复脚本（db_changes/V1.0.9_fix_l3_image_directory.py）：修复版本3中200条 image_resource 记录的 product/url/path 指向了深层条目而非L3产品的问题，同步更新 data_entry 描述中的URL引用，移动物理文件到正确L3目录

---

# 当前研发版本: V1.0.8 beta

## 功能变更说明

### 产品清单
- 预览审批状态同步修复：待审核、审核通过的清单项在预览中不再显示编辑/添加/删除按钮，与数据清单的审批流权限控制保持一致
- 平级拖拽落点修复：拖拽指示线从目标行底部改为目标行顶部，与实际插入位置一致
- 同级拖拽到末尾修复：鼠标拖到列表底部空白区域时正确识别为追加到末尾
- 新增"编码重排序"功能：选中一个或多个L3产品级别条目，弹窗中分别编辑编码前缀和名称，确认后自动对其所有子节点按新前缀重新顺序编号
- 版本划分交互增强：清单中新增"非标配"复选框（与曜/远/驰互斥）；最小集为"是"的系列复选框用绿色圆角矩形标记；取消系列勾选时自动清除最小集标记
- 选中逻辑优化：选中节点自动选中全部子节点，不再联动选中父节点；插入待生成清单时自动带上全部祖先节点确保树结构完整
- 插入弹窗取消修复：取消插入目标清单弹窗时正确重置"插入待生成清单"按钮的 loading 状态
- 新增右键菜单（复制/剪切/粘贴）：整行右键弹出菜单，支持复制、剪切、粘贴到同级、粘贴到下级（子级），支持多选和跨业务域操作，自动同步分类信息
- 右键菜单新增升级/降级/上移/下移四个按钮：升级降低层级，降级提升层级，上移/下移在同级相邻节点间交换排序
- 拖拽降级修复：nest 模式（偏右拖拽）改为调用 moveToSibling 而非 moveToParent，避免跨级跳变
- 拖拽指示线修复：同域最上方/最下方拖拽时指示线消失问题修复，fallback 搜索改为双向搜索
- 上移/下移 L3 级修复：L3 级节点 parentId 为 null 时正确查询同域根节点进行排序交换
- 编辑表单数据残留修复：切换编辑不同条目时，先重置表单再从API获取完整数据，避免招标参数等字段残留上一次编辑的内容
- 分隔行添加产品L1/L2缺失修复：从分隔行添加产品/系统时，按名称自动匹配业务分类和业务域ID，正确加载L1/L2下拉选项
- categoryId/domainId 自动补全：后端新建、编辑、Excel导入条目时，若 categoryId/domainId 为空但名称有值，自动按名称查询补全ID
- 新增备注标签展示：名称列后显示橙色圆形感叹号备注标签，悬浮显示备注内容（含章节名称）；未展开时子节点备注冒泡到父节点显示（递归全部后代），展开后标签移至各子节点
- 文档生成进度修复：Excel 生成添加进度更新；预设 totalEntries/processedEntries 避免 null；裸线程改为 Spring TaskExecutor 限制并发避免 SQLite 锁竞争；Excel 生成完成时确保 processedEntries == totalEntries，进度条正确显示 100%；自定义Tab生成文档弹窗重置输出格式为Excel避免误发Word请求；完成状态写入增加3次重试兜底；前端轮询增加30秒超时保护和错误日志；有未完成任务时禁止再次生成并提示
- 自定义清单排序与主清单同步：移除自定义清单独立排序机制，拖拽排序直接更新主数据 sortOrder，自定义清单显示顺序与主清单完全一致，预览和生成文档的顺序不再出现不一致

### 系统管理
#### 版本管理
- 版本创建重构为多步骤异步流程：创建新版本时弹窗展示每步进度和数据量，补齐产品分类（L1/L2/L3）、自定义清单的复制，新增分类ID回写步骤确保新版本数据指向正确分类记录
- 版本创建图片URL修复：步骤7改为替换所有历史版本号的URL（不再仅替换源版本号）；新增data-id映射（图片操作使用正确的新ID）；新增productId映射；消除BaseProduct双重复制；修复v6残留旧版本URL数据
- 新增删除版本功能：仅允许删除草稿状态的版本，删除前风险确认弹窗，分步展示清理进度，自动清理全部关联数据（清单、分类、选项、图片、自定义清单、文档生成记录）

#### 图库管理
- 图片目录层级修复：图床物理目录严格限制为 category/domain/product(L3) 三级结构；前端 imagePickerProduct 始终追溯 level=3 祖先；后端 upload/migrateDownload 增加 L3 校验；修复 v3/v4/v5 历史数据（444 条 image_resource + 150 条 entry URL）并迁移物理文件

#### 非常规操作
- 新增"SQL脚本执行"页签：支持手动输入SQL语句或上传.sql脚本文件直接执行，查询结果以动态表格展示，DDL/DML逐条反馈执行状态和耗时
- 数据库备份路径修复：备份数据库步骤改为从JDBC URL解析绝对路径，修复服务器部署时因工作目录不同导致备份跳过的问题

---

# V1.0.6 beta

## 功能变更说明

### 产品清单
- 图片选择器重构：移除左侧层级树，改为按业务域（domain）加载全部图片，修复子层级图片遗漏问题（如 1.2.1 下 1.2.1.1 的图片不再缺失）
- 图片选择器过滤修复：后端新增 `includeReferenced` 参数，避免跨领域引用图片混入选择器
- 图片选择器跨产品过滤修复：`loadImages` 传递 `product` 参数，按当前产品精确过滤（如编辑 5.3.9.1 时只显示 18 张图而非整个域的 387 张）
- 清单查询数据量优化：query 接口返回精简 DTO（排除功能说明、招标参数说明等大文本字段），JSON 体积从 10.8MB 减至 3.6MB，Tab 切换不再超时
- 编辑器图片改名同步修复：图床改名→编辑器同步、编辑器改名→图床同步，三阶段重构避免 SQLITE_BUSY 错误
- 编辑器关闭自动保存：编辑器关闭时非新建模式自动保存，防止改名后未保存导致数据不一致
- Word/预览图片布局优化：连续图片按横版/竖版分组渲染——横版各自独立一行，连续竖版并排显示

### 系统管理
#### 用户管理
- 用户列表新增在线状态列：基于内存 Map 实时追踪用户在线状态
- 用户列表新增最后登录时间列：记录用户最近一次登录时间
- 用户列表新增操作日志按钮：点击弹窗查看该用户所有数据库写操作记录
- 操作日志全模块埋点：需求管理（创建/更新/状态变更/删除）、文档生成（生成Word/Excel）、审批管理、版本管理（创建/发布/回滚）、非常规操作（迁移/同步/修复/重置）全部纳入日志记录
- 操作日志修复：日志写入改用 REQUIRES_NEW 独立事务，解决 SQLite 单写锁导致业务事务内日志写入静默失败的问题
- Word文档生成修复：空分类条目不再产生多余空白章节（自动向上查找父节点分类/域）
- Word文档生成新增"压缩图片"选项：勾选后图片按显示尺寸缩放+JPEG压缩，文档体积减小约90%
- Word压缩图片清晰度修复：压缩目标分辨率提高2.4倍（模拟150DPI打印质量）、插值算法改为BICUBIC、JPEG质量从0.85提升至0.92，消除压缩后图片严重失真问题
- 文档生成取消机制：删除正在生成中的记录时，生成线程自动检测取消标志并提前终止，避免后台空跑和数据库异常
- 文档生成进度修复：SQLite 添加 busy_timeout=30000 解决写锁冲突导致进度更新静默失败；进度更新频率从每条改为每 2%（约 50 次写库）；前端轮询间隔从 100ms 改为 2s 减少竞争
- 大文件下载超时修复：前端下载超时改为10分钟、Vite代理超时改为10分钟、后端Tomcat连接超时改为10分钟
- 操作日志描述增强：单条操作记录业务标题（如产品系统名称、需求标题、图片文件名、用户姓名），批量操作记录影响数量等关键信息
#### 图床管理
- 新增列表视图模式：支持在网格视图和列表视图间切换，列表模式以文件列表方式展示图片（名称/大小/操作），同样支持编辑、删除、引用查询、复制URL、替换等全部操作功能
- 服务器时区修复：JVM 默认时区设为 Asia/Shanghai，Jackson 序列化时区设为 Asia/Shanghai，解决服务器 EDT 时区导致审批日志、操作日志等时间显示比北京时间慢 12 小时的问题
#### 非常规操作
- 新增"修复图片引用ID"功能：批量修正历史遗留的无效 data-id 引用，修复 4338 个孤儿引用（修复率 99.99%）
- 新增"SQL脚本执行"页签：支持手动输入SQL语句或上传.sql脚本文件直接执行，结果逐条展示执行状态和耗时

---

# V1.0.5 beta

## 功能变更说明

### 产品清单
- 批量修改版本划分：批量操作菜单新增"版本划分"选项，支持批量设置曜/远/驰系列及对应最小集属性
- 图片重命名同步物理文件：重命名图片时同步修改磁盘文件名，确保物理文件名与显示名一致
- 非常规操作新增"同步图片文件名"：一次性将现有图片物理文件名同步为显示名

#### 图床管理
- 图片排序优化：图床和编辑器图片选择器按上传时间倒序排列，最新图片显示在最前面
- Word生成修复：修复本地图片（local:前缀）未正确嵌入Word文档的问题
- Word标题修复：修复图片重命名后Word中图片标题仍显示旧物理文件名的问题（改为使用显示名）
- 图片重命名引用同步修复：修复图床改名后自定义编辑器中图片丢失的问题，改用 data-id 精确定位 image-card 块进行引用同步，彻底解决历史数据 URL 格式不一致导致替换失败的问题

### 系统管理
#### 图床管理
- 图片加载遮罩：图床切换目录时增加加载等待遮罩效果，分批渲染避免动画卡顿
- 图片版本隔离架构改造：图片物理文件按版本ID独立存储，彻底解耦版本间文件依赖
  - 新增"非常规操作"菜单：提供图片版本隔离迁移功能（一键迁移/分步操作），支持迁移进度实时查看
  - 上传图片增加同版本同目录同名文件的唯一性检查
  - 创建新版本时物理文件独立复制，不再多版本共享同一文件
  - 删除图片时直接删除物理文件，不再需要跨版本引用检查

---

# V1.0.4 beta

## 功能变更说明

### 产品清单
- 预览性能优化：图片宽高信息存储到数据库，预览时直接从数据库读取，不再逐张HTTP下载图片获取尺寸，大幅提升加载速度
- 预览图片布局修复：修复连续竖版图片未并排排列的问题（尺寸判断从HTTP下载改为数据库读取，启动时自动回填已有图片宽高）
- 数据加载优化：主清单和自定义清单加载时增加"数据加载中"提示
- Word生成修复：本地图片改为从文件系统直接读取，修复部署到内网后Word中图片丢失的问题
- 图床计数修复：修复图床目录树产品节点图片计数不准确的问题（与右侧实际显示数量保持一致）
- 文档生成优化：功能说明文档生成增加"包含图片"复选框，取消勾选后生成纯文字Word文档
- 批量操作优化：点击确认后立即关闭操作弹窗，避免批量处理期间用户二次操作

---

# V1.0.3 beta

## 功能变更说明

### 产品清单
- 批量修改解决方案弹窗：增加替换/追加模式切换，替换模式覆盖原有值，追加模式将新值添加到原有解决方案中
- 清单编辑表单：业务分类、业务域下拉选择器改为所有级别可编辑
- 清单编辑表单：新增系统类型下拉选择器（数据来源于系统类型维护）
- 图片名称同步修复：图床重命名图片后，编辑器 image-card 中的名称自动同步更新
- 编辑器打开/保存时图片名称校准：自动将 image-card 的 filename 与 image_resource 表最新记录对齐，防止旧名称覆盖
- 预览图片修复：修复 image-card 格式 URL 含中文空格时预览不显示的问题（URL 空格编码为 %20）
- 预览 caption 修复：修复 inline URL caption 含空格时被截断的问题
- 版本切换弹窗修复：修复点击当前版本仍弹出确认框的问题
- 引用展示增强：图片引用列表新增版本号列，区分不同版本的引用
- 拖拽交互重构：拖拽层级变更改为鼠标左右移动控制——向左升级（绿色）、向右降级（橙色），指示线统一显示在行底部
- 拖拽升级修复：修复 L4→L3 升级操作不生效的问题（L3 节点 parent_id 为空时的 fallback 处理）
- 拖拽域校验修复：修复 findL2Ancestor 对 L3 节点返回 null 导致同域拖拽误报"只能在同一业务域内移动"的问题
- 拖拽层级变更后同步更新 image_resource 的 category/domain/product，修复图床计数为 0
- 预览图片URL修复：去掉硬编码 localhost:8080，改用相对路径，支持部署到任意服务器
- 预览图片说明文字：自动去除文件扩展名，只显示文件名主体
- 上传图片自动补扩展名：用户输入的显示名无扩展名时自动追加
- 插入图片后滚动定位：插入/替换图片后编辑器自动滚动到图片位置
- 拖拽升级修复：修复 L4→L3 升级操作不生效的问题（L3 节点 parent_id 为空时的 fallback 处理）
- 拖拽域校验修复：修复 findL2Ancestor 对 L3 节点返回 null 导致同域拖拽误报"只能在同一业务域内移动"的问题

### 需求管理
- 图片重命名时同步更新需求条目（ReqItem）中的引用名称

### 系统管理
#### 基础数据维护
##### 产品分类维护
- 产品分类改为两级结构：统计分类（L1）→ 核心业务产品（L2）
- 修正Excel导入逻辑：L2映射到第3列（核心业务产品），跳过第2列
- Excel导入同时导入系统归类到sys_option表（type=systemType）
- 支持拖拽改变显示顺序

##### 系统类型维护
- 新增系统类型维护菜单，维护系统归类选项（如：科研、数据管理、AI、集成等）
- 复用选项维护组件（OptionManage.vue），type=systemType
- Excel导入产品分类时同步导入系统类型数据

#### 图床管理
- 引用查询性能优化：图片引用查询从 findAll 全量加载改为 SQL LIKE 查询，大幅提升响应速度
- 引用按钮增加 loading 状态，防止用户误以为无响应
- 图床目录树修复：修复部分 L3 产品节点未显示在树中的问题（L3 节点 domain_id 为空时通过 colBizDomain 文本匹配 fallback）
- ImagePicker 点击左侧树节点时自动清空搜索框

---

# V1.0.2

## 功能变更说明

### 产品清单
- 数据层级修复：根据编号前缀段数自动修正 level、parent_id、is_leaf、domain_id、category_id
- 批量修改业务域弹窗：增加 L2 下子树选择功能，支持将选中条目移动到指定树节点下
- 批量修改业务域弹窗：修复子树不显示问题（后端改为递归加载完整树结构，前端去掉lazy模式改为一次性渲染）
- 批量修改业务域修复：不选树节点时只修改分类和域信息，不再错误地改变条目层级
- 批量修改业务域简化：移除弹窗中的L3级树列表，只保留业务分类和业务域选择
- 拖拽改变层级：在同一业务域下支持拖拽改变树层级
- 拖拽层级上移：支持将L4+节点拖回L3同级（sibling同级/nest移入子节点）
- 拖拽目标检测重构：改用滚动位置+行高纯计算确定目标行，修复虚拟滚动下拖拽只有第一行生效的问题
- 拖拽示意线：飞书风格动态缩进（排序=蓝色当前行缩进，下移层级=橙色多缩进一级，上移层级=绿色少缩进一级）
- 清单构建排序：后端查询改为按基础数据中分类/域的sort_order排序，而非字符串字典序
- 清单排序修复：前端buildTree不再对roots全局排序（sortOrder仅在同一L2内有效），分隔行顺序严格按基础数据的分类/域排序
- 自定义Tab排序修复：customTabEntry.sortOrder相同时按分类/域sort_order二级排序
- 左侧导航树排序修复：L1/L2分别按base_category/base_domain的sort_order排序
- 批量修改业务域后排序：被移动条目自动排到目标L2域末尾（sortOrder = 目标域L3最大值+1）
- 拖拽到域末尾修复：拖拽到同域最后一行下方时正确显示蓝色示意线并插入末尾
- 自定义Tab排序修复：hasNullSort分支初始排序改为按分类/域sortOrder，最终排序改为先按分类/域分组再按customTabEntry.sortOrder排序

### 系统管理
#### 用户管理
- 403响应处理：token过期时自动清除并跳转登录页，避免请求静默失败

#### 基础数据维护
##### 业务分类维护
- 登录页面版本号：从后端动态读取 VERSION.md，不再硬编码
